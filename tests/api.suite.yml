actor: ApiTester
modules:
    enabled:
        - REST:
              depends: PhpBrowser
              url: 'http://192.168.0.2:8000'
              part: [json, db]
        - Yii2:
              part: [orm, fixtures, db]
              entryScript: index-test.php
        - Db:
              dsn: 'mysql:host=localhost;dbname=ezh'
              user: 'xenon'
              password: 'starwars'